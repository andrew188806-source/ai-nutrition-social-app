# RA-2F-P2 application activation

The fixed Restaurant Web route governs only the existing branch-menu display-name override. It calls the frozen RA-2F-P1 preview and mutation RPCs, preserves `NULL` as no override, and requires explicit SET or CLEAR confirmation. It never edits canonical menu identity, descriptions, structured food authority, or predecessor control planes.

Development acceptance is deliberately disabled in this local-freeze round. Its authorized later cycle begins with `dev-bmi-b-main` at override `NULL`, version `2`, and canonical name `B Item`.
