#!/usr/bin/env node
import fs from "node:fs";import ts from "typescript";
const source=fs.readFileSync("apps/restaurant-web/runtime/restaurant-owner-visibility.ts","utf8"),js=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022}}).outputText,mod=await import(`data:text/javascript;base64,${Buffer.from(js).toString("base64")}`),valid={ok:true,state:"applied",branchMenuItemId:"dev-bmi-b-main",branchSpecificStatus:"hidden",branchSpecificStatusVersion:"3",auditId:"audit-private"},accepted=x=>mod.parseMutation(x)?.state==="ready",error=x=>mod.parseMutation(x)?.state;const cases=[["six-key applied DTO accepted",accepted(valid)],["available to hidden HTTP success DTO",accepted({...valid,branchSpecificStatus:"hidden",branchSpecificStatusVersion:"3"})],["hidden to available HTTP success DTO",accepted({...valid,branchSpecificStatus:"available",branchSpecificStatusVersion:"4"})],["missing state rejected",!accepted((( {state,...v})=>v)(valid))],["ready state rejected",!accepted({...valid,state:"ready"})],["success state rejected",!accepted({...valid,state:"success"})],["hidden state rejected",!accepted({...valid,state:"hidden"})],["arbitrary state rejected",!accepted({...valid,state:"other"})],["unknown success field rejected",!accepted({...valid,unexpected:true})],["version remains decimal string",accepted({...valid,branchSpecificStatusVersion:"9007199254740991"})],["stale maps bounded",error({ok:false,errorCode:"stale_state"})==="stale_state"],["no-change maps bounded",error({ok:false,errorCode:"no_change"})==="no_change"],["audit id is not browser DTO",!Object.hasOwn(mod.parseMutation(valid),"auditId")]];for(const[n,p]of cases)console.log(`${p?"PASS":"FAIL"} ${n}`);console.log(JSON.stringify({suite:"restaurant-owner-visibility-ra-2d-p2-r1-regression",total:cases.length,passed:cases.filter(([,p])=>p).length,failed:cases.filter(([,p])=>!p).length}));if(cases.some(([,p])=>!p))process.exitCode=1;
