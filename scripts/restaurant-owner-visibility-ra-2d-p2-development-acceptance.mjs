#!/usr/bin/env node
console.error("RA-2D-P2-R1 Development acceptance is intentionally disabled. The next separately authorized cycle starts available / version 4, proves 200 for available/v4→hidden/v5 and hidden/v5→available/v6, then verifies ABA stale and six audit rows. No database or HTTP mutation was attempted.");process.exitCode=1;
