#!/usr/bin/env node
// Every mutant is in memory. Repository files are never written.
import fs from "node:fs";const read=p=>fs.readFileSync(p,"utf8").replace(/\r\n/g,"\n");
const original={sql:read("supabase/migrations/20260913020000_staff_authority_p3_p6_p2d_b0_b_branch_mutation_authority.sql"),selector:read("apps/admin-web/auth/admin-protected-mutation-authority.ts"),runtime:read("apps/admin-web/server/platformAdminBranchStatusRuntime.ts"),staff:read("apps/admin-web/server/staffAdminBranchStatusMutation.ts"),transport:read("apps/admin-web/server/staffAdminBranchStatusMutationTransport.ts"),context:read("apps/admin-web/auth/admin-context.ts"),extraLegacyRewrite:false};const originalFn=original.sql.slice(original.sql.indexOf("create function public.staff_admin_set"),original.sql.indexOf("comment on function public.staff_admin_set"));
function audit(s){const fn=s.sql.slice(s.sql.indexOf("create function public.staff_admin_set"),s.sql.indexOf("comment on function public.staff_admin_set")),post=s.runtime.slice(s.runtime.indexOf("handlePlatformAdminBranchStatusMutationRequest"));return !s.extraLegacyRewrite&&fn===originalFn&&!/create\s+table[^;]*staff[^;]*receipt/i.test(s.sql)&&/lock_current_staff_branch_status_actor_v1\(\)/.test(fn)&&!/lock_current_platform_admin|source_type|bundle_key|migration_backfill/.test(fn)&&!/p_(?:actor|subject|auth_user)/.test(fn.slice(0,fn.indexOf(") returns")))&&/platform_admin_operation_receipts/.test(fn)&&/hashtextextended\(v_actor::text \|\| ':' \|\| p_request_id::text, 0\)/.test(fn)&&/actor_auth_user_id=v_actor and request_id=p_request_id/.test(fn)&&/idempotency_conflict/.test(fn)&&/p_expected_version/.test(fn)&&/for update/.test(fn)&&!/status_version\s*=/.test(fn)&&/operational_pause/.test(fn)&&/operational_resume/.test(fn)&&/grant select \(id, restaurant_id, name, status, status_version\), update \(status\)/.test(s.sql)&&!/grant (?:insert|delete|truncate)[^;]*restaurant_branches/i.test(s.sql)&&!/grant update[^;]*to authenticated/i.test(s.sql)&&!/grant select, insert, (?:update|delete)/i.test(s.sql)&&/revoke all on function public\.staff_admin_set[\s\S]*from public, anon, authenticated, authenticator, service_role/.test(s.sql)&&!/grant execute[^;]*(?:anon|service_role)/i.test(s.sql)&&/credentialMode === "bearer"[\s\S]*authority: "legacy"/.test(s.selector)&&s.selector.indexOf('credentialMode === "bearer"')<s.selector.indexOf("resolveAdminAuthorityMode(env)")&&/mutationAuthority\.authority === "staff"[\s\S]*mutateStaffAdminBranchStatus[\s\S]*mutatePlatformAdminBranchStatus/.test(post)&&/authorization\.mode === "browser_cookie_session" && !acceptsAdminApiCookieMutationOrigin\(request\)/.test(post)&&post.indexOf("acceptsAdminApiCookieMutationOrigin")<post.indexOf("resolveAdminProtectedMutationAuthority")&&/body = JSON\.parse\(text\);/.test(post)&&post.indexOf("JSON.parse(text)")<post.indexOf("resolveAdminProtectedMutationAuthority")&&!/catch[\s\S]{0,220}mutatePlatformAdminBranchStatus/.test(s.staff+post)&&/staff_admin_set_restaurant_branch_status_v1/.test(s.transport)&&/parseMutationResult\(raw, input\.requestId\)/.test(s.staff)&&!/staff_has_permission|readContext/.test(s.staff+s.transport)&&!/service_role/.test(s.staff+s.transport+s.runtime+s.selector)&&/authority\.context\.state !== "admin"/.test(s.context);}
const mutants=[];function mutate(name,key,anchor,replacement){if(!original[key].includes(anchor))throw new Error(`stale anchor ${name}`);mutants.push({name,state:{...original,[key]:original[key].replace(anchor,replacement)}});}
mutants.push({name:"rewrite legacy mutation RPC",state:{...original,extraLegacyRewrite:true}});mutate("parallel staff receipt table","sql","begin;","begin;\ncreate table admin_internal.staff_receipts(id uuid);");mutate("different advisory namespace","sql","v_actor::text || ':' || p_request_id::text","'staff:' || v_actor::text || ':' || p_request_id::text");mutate("skip receipt lookup","sql","select * into v_prior from admin_internal.platform_admin_operation_receipts","select * into v_prior from admin_internal.platform_admin_operation_receipts where false and");mutate("omit context authority","sql","lock_current_staff_branch_status_actor_v1()","lock_current_staff_branch_status_actor_without_context_v1()");mutate("use legacy actor helper","sql","lock_current_staff_branch_status_actor_v1()","lock_current_platform_admin_branch_status_actor_v1()");mutate("caller actor argument","sql","p_request_id uuid","p_request_id uuid, p_actor uuid");mutate("source specific authority","sql","v_actor := admin_internal.lock_current_staff_branch_status_actor_v1();","perform 'migration_backfill'; v_actor := admin_internal.lock_current_staff_branch_status_actor_v1();");mutate("grant service role execute","sql","to authenticated;","to authenticated, service_role;");mutate("grant anon execute","sql","to authenticated;","to authenticated, anon;");mutate("grant direct authenticated Branch update","sql","grant select (id, restaurant_id","grant update(status) on public.restaurant_branches to authenticated;\ngrant select (id, restaurant_id");mutate("grant extra Branch column update","sql","update (status)","update (status, name)");mutate("grant receipt update","sql","grant select, insert on table admin_internal.platform_admin_operation_receipts","grant select, insert, update on table admin_internal.platform_admin_operation_receipts");mutate("grant receipt delete","sql","grant select, insert on table admin_internal.platform_admin_operation_receipts","grant select, insert, delete on table admin_internal.platform_admin_operation_receipts");mutate("bypass CAS version","sql","or v_before_version <> p_expected_version","");mutate("remove target row lock","sql","where branch.id=p_branch_id for update","where branch.id=p_branch_id");mutate("remove advisory lock","sql","perform pg_catalog.pg_advisory_xact_lock","perform pg_catalog.hashtextextended");mutate("manual version increment","sql","set status=p_requested_status","set status=p_requested_status,status_version=status_version+1");mutate("broaden reason rule","sql","p_reason_code='operational_pause'","p_reason_code is not null");mutate("select staff for bearer","selector",'authority: "legacy" as const','authority: "staff" as const');mutate("bearer consults selector first","selector",'if (credentialMode === "bearer") {','resolveAdminAuthorityMode(env);\n  if (credentialMode === "bearer") {');mutate("staff cookie remains legacy","runtime","? mutateStaffAdminBranchStatus","? mutatePlatformAdminBranchStatus");mutate("fallback to legacy","staff","return failure(error);","return mutatePlatformAdminBranchStatus();");mutate("weaken CSRF","runtime","!acceptsAdminApiCookieMutationOrigin(request)","false && !acceptsAdminApiCookieMutationOrigin(request)");mutate("select before body parse","runtime","let body: unknown;","resolveAdminProtectedMutationAuthority(authorization.mode, env);\n  let body: unknown;");mutate("change body parser","runtime","JSON.parse(text)","JSON.parse(text.trim())");mutate("allow staff app admission","context",'authority.context.state !== "admin"','authority.context.state === "unauthenticated"');mutate("call permission fanout","staff","const raw = await transport.mutate","await transport.readContext(); const raw = await transport.mutate");mutate("change staff RPC name","transport","staff_admin_set_restaurant_branch_status_v1","platform_admin_set_restaurant_branch_status_v1");mutate("omit result parser","staff","parseMutationResult(raw, input.requestId)","raw");
const results=mutants.map(x=>({name:x.name,killed:!audit(x.state)})),survivors=results.filter(x=>!x.killed).map(x=>x.name);for(const r of results)console.log(`${r.killed?"PASS":"FAIL"} ${r.name}`);console.log("\n"+JSON.stringify({suite:"staff-authority-p3-p6-p2d-b0-b-mutations",mutations:results.length,killed:results.length-survivors.length,survivors:survivors.length,survivorNames:survivors,repositoryFilesWritten:0,databaseUsed:false,developmentAccessed:false,productionAccessed:false},null,2));process.exitCode=survivors.length?1:0;
